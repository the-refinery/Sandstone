function {$ControlName}_OnBlur()
{
	var isEntered;

	if ($('{$ControlName}').value == '')
	{
		isEntered = false;
	}
	else
	{
		isEntered = true;
	}

	if (isEntered)
	{
		RaiseAJAXevent('{$ParentControlName}', 'autocomplete', '{$ControlName}='+$F('{$ControlName}'));
	}
}