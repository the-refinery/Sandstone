<li id="{$ControlName}">
    <p class="meta">by {$Element->User->FirstName} {$Element->User->LastName} <em>{$Element->Timestamp->FriendlyDate}</em></p>

    {!?IsModerator,comment_delete.htm.template}

    <div class="MessageContent">
        {$MessageContent}
    </div>
</li>
